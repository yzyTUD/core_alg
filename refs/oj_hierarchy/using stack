#include <iostream>
#include <stack>
#include <queue>
using namespace std;
int main(){

    int a[]= {1,2,3,4,5,6};
    stack<int> s;
    for(int i=0;i<sizeof(a)/sizeof(*a);i++){
        s.push(a[i]);
    }

    int out=s.top();
    cout<<out<<endl;
    s.pop();
    cout<<s.top()<<endl;

    return 0;
}




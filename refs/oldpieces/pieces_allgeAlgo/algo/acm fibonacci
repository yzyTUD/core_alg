#include <iostream>
int main()
{
    int N = 13;
    int a[N];
    for (int i = 0; i < N; i++)
    {
        a[i] = 0;
    }
    a[0] = a[1] = 1;

    for (int i = 2; i < N; i++)
    {
        a[i] = a[i - 1] + a[i - 2];
    }

    //
    for (int i = 0; i < N; i++)
    {
        std::cout << a[i] << " ";
    }
    return 0;
}
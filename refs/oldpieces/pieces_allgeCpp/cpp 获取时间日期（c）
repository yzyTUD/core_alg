#include <stdio.h>
#include <time.h>
int main(int argc, char ** argv){

	time_t* now = new time_t;
	time(now);
	struct tm* pointer;
	pointer = localtime(now);
	
	printf("%d\n", pointer->tm_mon+1);
	printf("%d\n", pointer->tm_year + 1900);

	return 0;
}



		// Load all of the images by creating a GLTexture object and saving the pixel data.
		//for (auto& icon : icons) {
			GLTexture texture("icons/icon1");
			auto data = texture.load(resourcesFolderPath + "icons/icon1.png");
			mImagesData.emplace_back(std::move(texture), std::move(data));
		//}

		// Set the first texture
		auto imageView = new ImageView(imageWindow, mImagesData[0].first.texture());
		imageView->bindImage(mImagesData[0].first.texture());
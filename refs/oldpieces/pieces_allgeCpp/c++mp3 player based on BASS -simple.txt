#include <windows.h>
#include <stdio.h>
#include <math.h>
#include "bass.h"

int main() {

	BASS_Init(-1, 44100, 0, 0, NULL);
	int chan = BASS_StreamCreateFile(FALSE, "t.mp3", 0, 0, BASS_SAMPLE_LOOP);
	BASS_ChannelPlay(chan, FALSE);

	getchar();
	BASS_Free();
	return 0;
}